to drawSquare :size
    repeat 4 [
        forward :size
        right 90
    ]
end
to shrinkingSquares :size
    drawSquare :size
    if :size [
        shrinkingSquares difference :size 10
    ]
end

make "size 100
shrinkingSquares :size
